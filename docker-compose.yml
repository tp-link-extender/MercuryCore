version: "3"

# Config file for compose, a tool that allows for easily managing multiple docker containers.
# https://docs.docker.com/compose/

services:
    surreal:
        image: surrealdb/surrealdb:nightly
        ports:
            - 8000:8000
        restart: on-failure
        command: start -p root
